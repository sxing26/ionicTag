import { StationSearchPipe } from './station-search.pipe';

describe('StationSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new StationSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
